# 🚀 立即操作指南 - 让用户用上最新版本

## ⚠️ 当前问题

1. **开发版脚本有错误**：加载的是 `stable.js`，应该加载 `nightly.js`
2. **需要确保两个分支都有最新的构建文件**

## 📋 第一步：修复用户脚本

### 开发版脚本（已修复）

我已经为你创建了修复后的脚本：`酒馆助手脚本-【骰子系统】-自动更新开发版v3-已修复.json`

**修复内容**：
- ❌ 旧版本：`stable.js`（错误！）
- ✅ 新版本：`nightly.js`（正确！）

**新脚本内容**：
```json
{
  "content": "import('https://testingcf.jsdelivr.net/gh/jerryzmtz/my-tavern-scripts@nightly/dist/骰子系统/nightly.js');"
}
```

### 稳定版脚本（无需修改）

你的稳定版脚本已经是正确的，它会：
1. 获取最新的 tag 版本号
2. 加载 `stable.js`

## 📋 第二步：确保两个分支都有最新构建

### 方案 A：触发 GitHub Actions 自动构建（推荐）

1. **打开 GitHub 仓库**
   - 访问：`https://github.com/jerryzmtz/my-tavern-scripts`

2. **触发 main 分支构建**
   - 点击 `Actions` 标签页
   - 点击左侧的 `bundle` 工作流
   - 点击 `Run workflow` 按钮
   - 选择分支：`main`
   - 点击 `Run workflow`
   - 等待构建完成（约 2-5 分钟）

3. **触发 nightly 分支构建**
   - 再次点击 `Run workflow`
   - 选择分支：`nightly`
   - 点击 `Run workflow`
   - 等待构建完成（约 2-5 分钟）

### 方案 B：手动推送触发构建

在 `my-tavern-scripts` 仓库中执行：

```bash
# 1. 切换到 main 分支
cd I:\SillyTavern_Helper\my-tavern-scripts
git checkout main
git pull origin main

# 2. 创建一个空提交来触发构建（或者修改代码）
git commit --allow-empty -m "chore: 触发构建 stable.js"
git push origin main

# 3. 切换到 nightly 分支
git checkout nightly
git pull origin nightly

# 4. 创建一个空提交来触发构建
git commit --allow-empty -m "chore: 触发构建 nightly.js"
git push origin nightly
```

## 📋 第三步：验证构建结果

等待 GitHub Actions 完成后，验证文件是否存在：

### 检查 main 分支
访问：`https://github.com/jerryzmtz/my-tavern-scripts/tree/main/dist/骰子系统`
- ✅ 应该看到 `stable.js` 和 `stable.js.map`

### 检查 nightly 分支
访问：`https://github.com/jerryzmtz/my-tavern-scripts/tree/nightly/dist/骰子系统`
- ✅ 应该看到 `nightly.js` 和 `nightly.js.map`

### 测试链接是否可访问

**稳定版链接**（使用最新 tag）：
```
https://testingcf.jsdelivr.net/gh/jerryzmtz/my-tavern-scripts@main/dist/骰子系统/stable.js
```

**开发版链接**：
```
https://testingcf.jsdelivr.net/gh/jerryzmtz/my-tavern-scripts@nightly/dist/骰子系统/nightly.js
```

如果返回 404，等待 10-30 分钟让 jsDelivr 索引完成。

## 📋 第四步：更新用户脚本

### 给用户的说明

**稳定版用户**：无需修改，脚本会自动获取最新版本

**开发版用户**：需要更新脚本内容为：

```json
{
  "content": "import('https://testingcf.jsdelivr.net/gh/jerryzmtz/my-tavern-scripts@nightly/dist/骰子系统/nightly.js');"
}
```

或者直接使用我为你创建的修复版脚本文件。

---

## 🔄 以后如何更新？

### 日常开发（更新 Nightly）

```bash
cd I:\SillyTavern_Helper\my-tavern-scripts
git checkout nightly

# 修改代码
# ... 编辑 src/骰子系统/index.ts ...

git add .
git commit -m "feat: 添加新功能"
git push origin nightly
```

→ GitHub Actions 会自动构建，生成 `nightly.js`
→ 用户使用 `@nightly` 会自动获取最新版本

### 发布稳定版（更新 Stable）

```bash
cd I:\SillyTavern_Helper\my-tavern-scripts
git checkout main

# 方式 1：合并 nightly 的更改
git merge nightly

# 方式 2：直接修改 main 分支代码
# ... 编辑 src/骰子系统/index.ts ...

git add .
git commit -m "release: 发布稳定版"
git push origin main
```

→ GitHub Actions 会自动构建，生成 `stable.js`，并创建版本号 tag
→ 用户使用 tag 版本会自动获取最新版本

## 📝 关键要点

1. **两个分支完全独立**
   - 更新 nightly 不影响 stable
   - 更新 main 不影响 nightly

2. **自动构建**
   - 推送代码后，GitHub Actions 自动构建
   - 无需手动操作

3. **文件命名**
   - main 分支 → `stable.js`
   - nightly 分支 → `nightly.js`

4. **用户脚本**
   - 稳定版：使用 tag 版本或 `@main/dist/骰子系统/stable.js`
   - 开发版：使用 `@nightly/dist/骰子系统/nightly.js`

## ❓ 常见问题

### Q: 构建失败了怎么办？

**A:** 
1. 在 GitHub Actions 中查看错误日志
2. 检查 `src/骰子系统/index.ts` 是否有语法错误
3. 修复后重新推送

### Q: 用户说链接 404？

**A:**
1. 确认文件在 GitHub 上存在
2. 等待 10-30 分钟让 jsDelivr 索引
3. 或者使用版本号 tag（缓存更快）

### Q: 如何查看当前版本？

**A:**
- main 分支：查看 GitHub Releases 或 Tags
- nightly 分支：总是最新版本，使用 `@nightly`
